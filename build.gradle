apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src/java'
        }
        resources {
            srcDir 'res'
        }
    }
}

dependencies {
    compile fileTree(dir: 'lib', include: ['*.jar'])
}
