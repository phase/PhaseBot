apply plugin: 'java'

sourceSets {
    main {
        java {
            srcDir 'src'
            testDir 'test'
        }
        resources {
            srcDir 'res'
        }
    }
}

dependencies {
    compile fileTree(dir: 'lib', include: ['*.jar'])
}
